# Workout

Mobile-first workout playlist app hosted as a standalone GitHub Pages project.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The production app is configured for `https://mister-jp.github.io/workout/`.
